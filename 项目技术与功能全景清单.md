# 碳融智核项目技术与功能全景清单（文书版）

> 适用对象：项目申报书、技术说明书、答辩材料撰写人员  
> 代码基线：当前工作区主代码（不含“旧版备份”目录）  
> 生成时间：2026-04-16

---

## 1. 项目定位与总体架构

### 1.1 项目定位
碳融智核是一个面向中小企业的 ESG 与碳管理平台，覆盖从数据采集、票据识别、碳核算、风险评估、AIGC 报告生成到绿色金融对接的完整闭环。

### 1.2 架构分层
1. 前端展示层：HTML/CSS/JavaScript + ECharts/Chart.js + Three.js（可选）
2. API 服务层：FastAPI 提供业务接口、鉴权、跨域、数据聚合
3. 业务算法层：碳核算策略引擎、Isolation Forest 风险检测、时序预测、证据链服务、LLM/VLM 服务
4. 数据持久层：MySQL + SQLAlchemy ORM
5. AI 能力层：DashScope（Qwen-Plus / Qwen-VL）+ 本地 Mock 回退

---

## 2. 技术栈与依赖清单

## 2.1 后端核心技术
1. FastAPI（接口框架）
2. Uvicorn（ASGI 服务）
3. SQLAlchemy（ORM）
4. PyMySQL（MySQL 驱动）
5. Pydantic + pydantic-settings（数据校验与配置）
6. python-jose + passlib + bcrypt（JWT 鉴权与密码安全）
7. python-dotenv（环境变量管理）
8. aiofiles（异步文件能力）

## 2.2 AI/算法相关依赖
1. dashscope（大模型/多模态模型调用）
2. pypdf（PDF 解析）
3. numpy（数值计算）
4. scikit-learn（Isolation Forest、RandomForest）
5. joblib（模型持久化）
6. xgboost（可选，预测优先模型；未安装时自动回退 RandomForest）

## 2.3 前端技术与第三方库
1. Bootstrap 5.3（响应式 UI）
2. Font Awesome（图标）
3. ECharts 5.4.2（业务图表、预测图、风险图）
4. Chart.js + chartjs-plugin-datalabels（辅助图表）
5. html2pdf.js（报告导出 PDF）
6. Three.js + OrbitControls（3D 数字孪生可视化）

## 2.4 运行环境要求
1. Python 3.10+
2. MySQL 8.0+
3. 浏览器（Chrome/Edge 等现代浏览器）

---

## 3. 代码结构与职责分工

## 3.1 前端主文件
1. index.html：页面结构、模块容器、脚本引入入口
2. style.css：全局样式与交互视觉
3. api.js：统一 API 客户端（含 token、请求封装、业务接口）
4. script.js：通用页面逻辑（路由切换、登录页、toast、表单等）
5. app.js：启动入口与全局状态初始化
6. router.js：页面路由切换逻辑
7. auth.js：会话恢复、用户信息加载等认证侧逻辑
8. demo-flow.js：演示主流程（OCR -> 核算 -> 风控 -> 报告 -> 预测 -> 导出）
9. twin3d.js：3D 碳排数字孪生展示

## 3.2 后端主文件
1. backend/main.py：应用入口、CORS、路由注册、健康检查
2. backend/config.py：配置项与 DATABASE_URL 组装
3. backend/database.py：数据库引擎与 Session 管理
4. backend/models/*：业务数据模型
5. backend/routers/*：业务接口路由
6. backend/services/*：核心服务与算法实现

---

## 4. 后端 API 模块与功能矩阵

> 以下均为主代码路由（backend/routers），按前缀归类。

## 4.1 认证模块（/api/auth）
功能：注册登录、验证码、用户信息维护、访客预览。

核心接口：
1. GET /captcha
2. POST /sms-code
3. POST /login/phone
4. POST /login/enterprise
5. POST /login
6. POST /register
7. GET /me
8. PUT /me
9. GET /visitor-preview

## 4.2 碳核算模块（/api/carbon）
功能：排放因子与行业基准查询、碳核算计算、结果查询、Sankey 碳流与可解释链路。

核心接口：
1. GET /emission-factors
2. GET /industry-benchmarks
3. GET /records
4. POST /calculate
5. GET /result/{analysis_id}
6. GET /flow/{analysis_id}
7. GET /explain/{analysis_id}
8. GET /records/{record_id}
9. DELETE /records/{record_id}

## 4.3 ESG 模块（/api/esg）
功能：E/S/G 维度评分、综合评分、历史记录、趋势与对标、改进计划。

核心接口：
1. POST /calculate/environment
2. POST /calculate/social
3. POST /calculate/governance
4. POST /calculate/total
5. GET /history
6. GET /history/{record_id}
7. GET /score-detail
8. GET /benchmark
9. GET /improvement-plans
10. GET /trends

## 4.4 金融模块（/api/finance）
功能：金融产品浏览、申请、进度追踪、推荐、方案对比、材料上传。

核心接口：
1. GET /products
2. GET /products/{product_id}
3. POST /apply
4. GET /applications
5. GET /recommendations
6. GET /progress/{application_id}
7. GET /comparison
8. GET /materials
9. POST /materials
10. POST /batch-apply

## 4.5 报告模块（/api + /reports/*）
功能：报告模板、生成、导出、审核、自定义模板、政策案例、咨询提交。

核心接口：
1. GET /reports/templates
2. POST /reports/generate
3. GET /reports
4. POST /reports/generate-ai
5. POST /reports/preview-context
6. POST /reports/export
7. GET /reports/export-records
8. POST /reports/{report_id}/save-cloud
9. GET /reports/my-reports
10. GET /reports/review-list
11. PUT /reports/{report_id}/review
12. GET /reports/custom-templates
13. POST /reports/custom-templates
14. GET /reports/{report_id}
15. GET /policies
16. GET /cases
17. POST /contact

## 4.6 OCR 模块（/api/ocr）
功能：票据识别、示例回传、链路核验与总览。

核心接口：
1. GET /health
2. POST /recognize
3. GET /sample/{doc_type}
4. GET /chain-check/{raw_data_id}
5. GET /chain-overview

## 4.7 风控模块（/api/risk）
功能：Isolation Forest 异常检测 + 风险解释 + 证据留痕。

核心接口：
1. POST /detect

## 4.8 证据链与首页图谱模块（/api）
功能：首页图谱、事件流、证据链写入/查询/验真、Merkle 证明、演示一键运行。

核心接口：
1. GET /home/graph
2. GET /events/latest
3. POST /evidence/store
4. GET /evidence/chain/{record_id}
5. POST /evidence/verify
6. GET /evidence/proof/{leaf_id}
7. POST /demo/run-once

## 4.9 预测模块（/api/forecast）
功能：月度碳排趋势预测 + Conformal 区间 + 高风险月份预警。

核心接口：
1. POST /monthly-carbon

## 4.10 企业中心模块（/api/enterprise）
功能：企业档案、资质、权限、账号、活动日志、企业看板。

核心接口：
1. GET /dashboard
2. GET /profile
3. PUT /profile
4. GET /certifications
5. POST /certifications
6. PUT /certifications/{cert_id}
7. DELETE /certifications/{cert_id}
8. GET /permissions
9. GET /account
10. PUT /account
11. GET /activities

## 4.11 碳管理模块（/api/carbon-manage）
功能：上传管理、手工录入、能耗分析。

核心接口：
1. GET /uploads
2. POST /upload
3. DELETE /uploads/{upload_id}
4. POST /manual-entry
5. GET /energy-analysis

## 4.12 驾驶舱模块（/api/dashboard）
功能：监控看板、结构分析、ESG 看板、关键指标。

核心接口：
1. GET /emission-monitor
2. GET /energy-structure
3. GET /esg-board
4. GET /key-indicators

---

## 5. 核心算法与 AI 能力

## 5.1 碳核算引擎（策略模式）
实现位置：backend/services/carbon_engine/

能力：
1. 按企业类型选择策略（Default/CrossBorder/DailyGoods）
2. 多活动类型核算（电、气、柴油、物流、仓储、包装等）
3. Scope1/2/3 分类汇总
4. 因子匹配回退机制（精确匹配 -> 地域回退 -> 行业回退 -> 默认0）
5. 不确定性传播（DQI 映射 + 95% 区间）
6. 生成碳流图与解释链路

## 5.2 风险检测（Isolation Forest）
实现位置：backend/services/risk_detector.py

能力：
1. IsolationForest + StandardScaler
2. 11维输入特征（基础 + P0 增强）
3. 风险分 0-100 归一化
4. 异常原因中文解释
5. 优化建议自动生成
6. 特征重要性输出

## 5.3 AIGC 报告（Qwen-Plus Structured）
实现位置：backend/services/report_llm.py

能力：
1. 结构化输入（排放、风控、证据链、同比趋势）
2. 结构化输出（summary/key_findings/risk_explanation/trust_statement/optimization_suggestions）
3. JSON 容错解析（直接 JSON、代码块提取、正则兜底）
4. 真模型与 Mock 双模式自动切换

## 5.4 VLM/OCR 识别与兜底
实现位置：backend/services/vlm_parser.py

能力：
1. 图片/PDF 多模态识别
2. 文档复杂度判别（simple/standard/complex）
3. 优先 VLM，高复杂场景兜底 simple heuristic
4. parse_method/complexity/parse_warning 透明输出
5. Mock 模式保障演示稳定性

## 5.5 时序预测与不确定性区间
实现位置：backend/services/forecasting.py

能力：
1. 滑窗监督学习（默认窗口3）
2. 模型选择：XGBoost 优先，缺包自动回退 RandomForest
3. Split-Conformal 区间（alpha 可调）
4. 动态阈值（均值+1.5*标准差）
5. 高风险月份自动标记
6. 历史样本不足时使用 MeanBaseline 回退

## 5.6 证据链与可信度评分
实现位置：backend/services/evidence_chain.py

能力：
1. 规范化 JSON + SHA256 哈希
2. 链式摘要（prev_hash -> current_hash）
3. Merkle Root 与 Merkle Proof
4. 链路完整性与证明校验
5. TrustScore 分解评分（完整性、哈希、时间、签名维度）

## 5.7 OCR 后数据治理
实现位置：backend/services/data_governance.py

能力：
1. 字段映射（FIELD_MAP）
2. 单位归一（UNIT_MAP）
3. 合法性校验（负值/零值/异常大值）
4. 统一活动记录结构输出

---

## 6. 数据模型与数据链路

## 6.1 模型层覆盖
目录：backend/models/

关键模型组：
1. 用户与企业：user.py、enterprise.py
2. 碳核算：carbon.py、analysis.py、analysis_v2.py
3. ESG：esg.py
4. 金融：finance.py
5. 报告：report.py、report_record.py
6. 数据管道：data_pipeline.py、data_upload.py
7. 证据链：flow.py、trace.py
8. 因子匹配日志：factor_match_log.py

## 6.2 业务链路（端到端）
1. 票据上传（OCR）
2. OCR/VLM 识别与字段提取
3. 数据治理（映射、单位、校验）
4. 活动数据标准化入库
5. 碳核算（因子匹配+Scope分解+不确定性）
6. 风险检测（Isolation Forest + 解释）
7. AIGC 报告生成
8. 证据链写入与验真
9. 趋势预测与风险预警
10. 报告导出与金融申请

---

## 7. 前端功能清单（按用户可见能力）

## 7.1 通用体验能力
1. 登录/注册/访客预览
2. 页面路由切换（hash + data-page）
3. Toast 提示、动态弹窗
4. 主题切换与本地存储
5. 侧边栏交互与响应式布局

## 7.2 业务能力
1. OCR 票据识别与结果展示
2. 碳核算触发与图表展示
3. ESG 维度评分与雷达图
4. 风险检测与证据链展示
5. AI 结构化报告生成与打字机渲染
6. 预测图（历史+预测均值+区间+阈值+高风险点）
7. 区间模式切换（Conformal/经验区间）
8. PDF 一键导出
9. 金融产品匹配与申请流程
10. 3D 碳排数字孪生展示（可选）

## 7.3 前端 API 封装特性（api.js）
1. 统一 BASE_URL 与 request 封装
2. 自动带 token（Authorization）
3. 统一错误处理（Failed to fetch/业务错误）
4. GET/POST/PUT/DELETE/upload 标准化
5. 覆盖认证、碳核算、ESG、金融、报告、OCR、风控、证据链、预测

---

## 8. 部署与运行说明（文档可直接引用）

## 8.1 后端
1. 进入 backend 目录
2. 创建并激活虚拟环境
3. pip install -r requirements.txt
4. 配置 .env（数据库、JWT、CORS、DASHSCOPE_API_KEY）
5. 初始化数据库（init_db.py）
6. 启动：uvicorn main:app --reload --host 0.0.0.0 --port 8000

## 8.2 前端
1. 项目根目录启动静态服务（推荐）：python -m http.server 5500
2. 浏览器访问：http://127.0.0.1:5500/index.html
3. 注意：避免 file:// 方式打开页面，否则会出现跨域问题

## 8.3 可观测入口
1. 健康检查：/health
2. Swagger：/docs
3. ReDoc：/redoc

---

## 9. 项目亮点（适合写入申报材料）

1. “核算 + 风控 + 报告 + 证据链 + 预测”一体化闭环
2. 碳核算引擎采用策略模式，支持行业场景扩展
3. 风控输出不仅有分数，还有可解释原因与优化建议
4. 报告采用结构化 LLM 输出，适配业务落地
5. 票据识别具备多层降级机制，保证可用性
6. 证据链支持哈希链与 Merkle 证明，强化可信披露
7. 预测模块支持 Conformal 不确定性区间，适合答辩展示
8. 前端具备完整演示流程与高可视化表达（图表+3D）

---

## 10. 已知行为与撰写注意事项

1. 预测模块在历史样本不足时会回退到 MeanBaseline，这是设计行为，不是故障。
2. 报告模块在未配置 DASHSCOPE_API_KEY 时会回退 Mock 结果。
3. 控制台中文乱码通常是终端编码显示问题，不代表接口逻辑错误。
4. 当前仓库存在“旧版备份_碳融智核_完整项目”目录，文书应以主目录代码为准。
5. 前端脚本存在历史演进痕迹（部分函数重复定义），但主链路已可运行。

---

## 11. 可直接引用的一句话项目描述（文书模板）

碳融智核是一套基于 FastAPI + MySQL + AI 大模型（Qwen）的中小企业 ESG 与碳管理平台，提供从票据识别、数据治理、碳核算、风险评估、证据链验真到智能报告与时序预测预警的全流程数字化能力，能够支撑企业绿色披露与绿色金融申报场景。
