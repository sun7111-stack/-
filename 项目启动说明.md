# 碳融智核项目启动说明（前后端）

## 1. 项目路径
- 项目根目录：`D:\WeChat Files\wxid_g050qj3i8au322\FileStorage\File\2026-02\碳融智核（改进版）\碳融智核（改进版）\碳融智核_完整项目`（你自己存放的地方）
- 后端目录：`D:\WeChat Files\wxid_g050qj3i8au322\FileStorage\File\2026-02\碳融智核（改进版）\碳融智核（改进版）\碳融智核_完整项目\backend`（后端backend存放的地方）

## 2. 启动后端（终端1）
```powershell
cd "D:\WeChat Files\wxid_g050qj3i8au322\FileStorage\File\2026-02\碳融智核（改进版）\碳融智核（改进版）\碳融智核_完整项目\backend"

python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

出现以下日志表示后端成功：
- `Uvicorn running on http://0.0.0.0:8000`
- `Application startup complete.`

### 启动前建议先做自检（可选）
在 `backend` 目录执行：

```powershell
python bootstrap.py
```

出现以下输出表示通过：
- `[BOOTSTRAP] PASS: 导入、告警、关键路由检查全部通过`

## 3. 启动前端（终端2）
```powershell
cd "D:\WeChat Files\wxid_g050qj3i8au322\FileStorage\File\2026-02\碳融智核（改进版）\碳融智核（改进版）\碳融智核_完整项目"
python -m http.server 5500
```

## 4. 浏览器访问地址
- 前端页面：`http://127.0.0.1:5500/index.html`
- 后端接口：`http://127.0.0.1:8000`
- API 文档：`http://127.0.0.1:8000/docs`

## 5. 常见问题
- 不要用 `file:///.../index.html` 直接双击打开，否则会出现接口请求失败。
- 如果虚拟环境激活报权限问题，先执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

再执行：

```powershell
.\.venv\Scripts\Activate.ps1
```

## 6. 关闭服务
- 在两个终端分别按 `Ctrl + C`。

## 7. Docker 一键部署（服务器推荐）

项目已提供 Docker 部署文件：

- `Dockerfile`：前端 Nginx 静态站点镜像
- `backend/Dockerfile`：后端 FastAPI 镜像
- `docker-compose.yml`：前端 + 后端 + MySQL 一键编排
- `.env.docker.example`：服务器环境变量示例
- `nginx.conf`：前端静态服务与 `/api` 反向代理配置

### 7.1 准备环境变量

在项目根目录执行：

```bash
cp .env.docker.example .env
```

然后编辑 `.env`，至少建议修改：

```bash
DB_PASSWORD=你的数据库密码
MYSQL_ROOT_PASSWORD=你的Root密码
JWT_SECRET_KEY=换成一串足够长的随机字符串
DASHSCOPE_API_KEY=你的通义千问DashScope Key
```

### 7.2 一键启动

```bash
docker compose up -d --build
```

首次启动会自动等待 MySQL 就绪，并执行：

```bash
python init_db.py
python migrate_db.py
```

### 7.3 访问地址

- 前端页面：`http://服务器IP:5500/index.html`
- 后端接口：`http://服务器IP:8000`
- API 文档：`http://服务器IP:5500/docs` 或 `http://服务器IP:8000/docs`

前端容器会把 `/api` 自动反向代理到后端容器，所以服务器部署时不会再依赖浏览器访问本机的 `localhost:8000`。

### 7.4 常用命令

```bash
# 查看运行状态
docker compose ps

# 查看日志
docker compose logs -f backend
docker compose logs -f frontend

# 停止服务
docker compose down

# 停止并删除数据库数据（谨慎）
docker compose down -v
```
