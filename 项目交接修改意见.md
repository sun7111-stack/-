# 碳融智核项目交接修改意见

## 一、当前项目状态

当前系统已经从原来的纯前端展示，升级为一个具备真实后端、数据库和业务闭环的碳核算与绿色金融服务平台。

系统目前已经具备以下能力：

```text
前端页面展示
FastAPI 后端接口
MySQL 数据库
真实业务补充数据
OCR / VLM 票据识别接口
碳核算接口
ESG 评分数据
风控检测接口
报告生成接口
证据链存证接口
Real Mode 真实运行模式
Demo Mode 演示模式
```

目前系统不是纯 Mock。前端保留了 Demo 演示模式，但已经新增真实运行模式，可以通过后端 API 和数据库完成：

```text
票据/示例凭证
-> OCR 识别
-> 活动数据标准化
-> 碳排放核算
-> 风控检测
-> 证据链存证
-> AI 报告生成
```

后续同学接手时，不建议大规模重写，而应在现有基础上继续做“真实化、稳定化、产品化”的完善。

## 二、已经完成的重要改动

### 1. 真实运行模式

新增文件：

```text
real-mode.js
```

作用：

```text
独立于 demo-flow.js，专门负责真实 API 闭环。
```

该模式不会自动使用 Mock。如果后端失败，会显示错误，不会伪装成功。

### 2. 首页动态化

新增文件：

```text
home-dynamics.js
```

已实现：

```text
首页力导向数据流转图
KPI hover 联动节点高亮
首页实时业务事件流
碳足迹拓扑图
区块链证据链业务化展示
```

注意：左侧导航顺序后来按需求恢复为原来的顺序，`home-dynamics.js` 里保留了重组侧边栏的函数，但没有启用。

### 3. 数据库补充数据

新增脚本：

```text
backend/import_supplement_data.py
```

已导入 Word 文件中的 2026 年 2-4 月补充数据。

新增数据包括：

```text
企业信息
原始凭证
OCR 解析字段
活动数据
排放因子
行业基准
碳核算结果
ESG 评分
风控结果
报告记录
首页事件流
证据链记录
```

### 4. 导入后数据库状态

关键表当前数量为：

```text
users: 11
enterprise_profiles: 10
raw_data_record: 26
parsed_data_record: 54
activity_record: 29
emission_factor_items: 34
analysis_results: 35
analysis_breakdowns: 47
industry_benchmarks: 12
esg_scores: 6
risk_results: 108
report_records: 12
event_stream: 35
evidence_chain_record: 205
```

## 三、不要随便删除的文件

接手同学不要随便删以下文件：

```text
api.js
app.js
demo-flow.js
real-mode.js
home-dynamics.js
visual-system.js
backend/import_supplement_data.py
```

各文件作用如下：

```text
demo-flow.js
```

负责原有演示模式。

```text
real-mode.js
```

负责真实 API 闭环。

```text
home-dynamics.js
```

负责首页动态图、事件流、拓扑图和证据链视觉增强。

```text
visual-system.js
```

负责图表主题、模态框修复和整体视觉统一。

## 四、后续最优先修改建议

### 1. 进一步稳定真实运行模式

当前真实模式已经可以跑通，但仍建议继续加强：

```text
后端健康检查更细化
OCR 服务状态展示
数据库连接状态展示
AI 模型状态展示
错误提示中文化
真实上传票据失败时给出明确处理建议
```

建议增加一个统一状态面板：

```text
后端 API：在线
MySQL：已连接
OCR/VLM：可用
因子库：已加载
证据链：可写入
报告服务：可用
```

这样答辩时会更有说服力。

### 2. 首页动态图继续优化

目前首页已经有数据流转图，但还可以更精致。

建议方向：

```text
节点尺寸再缩小
节点颜色更克制
避免节点文字拥挤
保留拖动联动效果
减少默认 ECharts 味道
KPI hover 联动更明显
```

首页主视觉建议保持：

```text
企业 -> 凭证 -> OCR -> 碳核算 -> 风控 -> 区块链 -> 绿色银行 -> 金融产品
```

但不要做成炫酷大屏，要保持学术型 SaaS 风格。

### 3. 碳核算页继续替换 3D 粒子展示

目前已经新增碳足迹拓扑图，原黑色 3D 数字孪生作为辅助保留。

后续建议：

```text
弱化 Three.js 黑色粒子风格
重点展示碳流向 Sankey 图
把能源采购、生产环节、运输、包装、Scope1/2/3 做成更清晰的流向图
每个图表增加一句结论型说明
```

建议表达：

```text
外购电力 / 燃料 / 物流 / 包装
-> 采购端 / 生产环节 / 仓储配送
-> Scope 1 / Scope 2 / Scope 3
-> 总碳排
```

### 4. 区块链展示继续业务化

现在已经有证据链时间线，但还可以进一步降低技术门槛。

不要只展示：

```text
Hash
Merkle Root
tx_id
```

应该重点展示：

```text
数据原始采集
OCR 识别完成
数据标准化完成
碳核算完成
风控扫描完成
链上存证成功
全流程核验通过
```

Hash 可以放在展开详情里，而不是页面主视觉。

### 5. 数据库和后端建议

现在数据库已经有补充数据，但后续建议继续完善：

```text
扩展 emission_factor_items
补充更多行业 benchmark
增加更多 raw_data_record 样本
让首页事件流从 /api/events/latest 拉取真实数据
让首页 KPI 从数据库统计生成
增加数据导入去重日志
```

目前 `backend/import_supplement_data.py` 已经具备幂等导入能力。再次运行 dry-run 不会重复导入。

### 6. 前端应减少硬编码数据

目前前端仍有不少硬编码数据，例如：

```text
首页 KPI 数字
首页事件流部分模拟数据
图表初始数据
部分报告预览内容
```

后续建议逐步改成从后端接口获取：

```text
/api/dashboard/...
/api/events/latest
/api/carbon/records
/api/reports/my-reports
/api/forecast/monthly-carbon
```

目标是让页面看起来和数据库真正联动。

## 五、运行方式建议

后端启动：

```bash
cd backend
python main.py
```

或：

```bash
cd backend
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

前端建议不要用 `file://` 打开，建议用：

```bash
python -m http.server 5500
```

然后访问：

```text
http://localhost:5500
```

## 六、数据库导入脚本说明

如果需要重新导入补充数据：

```bash
python backend/import_supplement_data.py
```

这是 dry-run，不会写库。

真正写库：

```bash
python backend/import_supplement_data.py --apply
```

注意：现在脚本已经做了幂等处理，重复运行一般不会重复插入同一批数据。

## 七、演示账号

原有账号：

```text
demo@carbon-ai.com
demo123
```

新增补充企业账号：

```text
supplement-e003@carbon.local
supplement-e004@carbon.local
supplement-e005@carbon.local
supplement-e006@carbon.local
supplement-e007@carbon.local
supplement-e008@carbon.local
```

默认密码：

```text
demo123
```

对应企业：

```text
鑫源新材料有限公司
味鲜食品有限公司
云智科技有限公司
盛华化工有限公司
速通物流有限公司
绿建建材有限公司
```

## 八、答辩表述建议

后续同学答辩时不要说：

```text
这是完全商用级系统
已经接入真实金融机构
已经是真实联盟链
```

更稳妥的说法是：

```text
本系统已经实现项目验收级真实可运行闭环，前端保留演示模式，同时提供真实运行模式。真实模式下可连接 FastAPI 后端和 MySQL 数据库，完成票据识别、活动数据标准化、碳核算、ESG 评分、风控检测、报告生成和证据链存证。当前系统已导入 2026 年 2-4 月补充业务数据，可支撑典型中小企业场景演示；后续若进入生产环境，还需扩展因子库、真实联盟链节点和风控模型训练数据。
```

## 九、后续最建议的分工

如果还有时间，建议按这个顺序分工：

```text
同学 A：负责真实接口联动，把首页 KPI 和事件流改成后端拉取
同学 B：负责碳核算页图表优化，重点做 Sankey 和行业对比图
同学 C：负责风控与证据链页面，把 Hash 展示改成业务时间线
同学 D：负责报告中心和导出，完善 PDF 样式和报告模板
```

## 十、总结

当前项目已经具备真实应用原型能力，不建议推倒重来。

后续重点不是继续堆页面，而是：

```text
减少硬编码
增强真实 API 联动
提高图表学术感
强化证据链业务表达
补充更多排放因子和行业基准
```

这样系统会从“能演示”进一步变成“能验收、能说明、能扩展”的完整项目。
