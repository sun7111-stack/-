# 项目交接修改意见

## 一、当前项目状态

当前系统已经从“纯前端展示页”升级为一个具备后端接口、数据库、业务数据和真实运行闭环的碳核算与绿色金融服务平台原型。

系统目前具备以下能力：

```text
前端页面展示
FastAPI 后端接口
MySQL 数据库与真实业务补充数据
OCR / VLM 票据识别接口
自动化碳核算接口
ESG 评分数据
绿色金融风控检测接口
AI 诊断报告生成接口
证据链 / 区块链存证接口
Real Mode 真实运行模式
Demo Mode 演示模式
```

需要明确的是：当前系统不是完全纯 Mock。前端保留 Demo 演示模式，同时新增了 Real Mode 真实运行模式。Real Mode 可以通过后端 API 与数据库完成下面这条闭环：

```text
票据 / 后端示例凭证
-> OCR 识别
-> 活动数据标准化
-> 碳排放核算
-> 风控检测
-> 证据链存证
-> 证据链核验
-> AI 报告生成
```

后续同学接手时，不建议推倒重写，而应该在现有基础上继续做“真实化、稳定化、产品化”。

## 二、已经完成的重要改动

### 1. 真实运行模式

新增 / 重点文件：

```text
real-mode.js
backend/routers/system.py
```

作用：

```text
real-mode.js：前端真实 API 闭环入口，独立于 DemoState / demo-flow.js。
backend/routers/system.py：后端真实模式健康检查接口，只读检查数据库、OCR、因子库、证据链和报告服务。
```

新增接口：

```http
GET /api/system/real-mode-health
```

该接口会返回：

```text
status: ok / warning / blocked
数据库状态
排放因子库状态
OCR/VLM 状态
证据链表状态
报告服务状态
关键业务表数据量
```

Real Mode 的原则是：

```text
不会自动伪装成 mock 成功
关键依赖异常时会阻断运行
OCR / 报告服务如果处于降级模式，会明确显示“降级”
成功后会展示 record_id、analysis_id、tx_id、Merkle Root 等后端返回证据
```

### 2. 首页动态化

新增 / 重点文件：

```text
home-dynamics.js
```

已经实现：

```text
首页力导向数据流转图
KPI hover 联动节点高亮
首页实时业务事件流
碳足迹拓扑图
区块链证据链业务化展示
```

注意：左侧导航顺序后来按需求恢复为之前顺序。`home-dynamics.js` 中保留了重组侧边栏的函数，但目前不要启用，避免和现有导航顺序冲突。

### 3. 数据库补充数据

新增脚本：

```text
backend/import_supplement_data.py
```

已导入 Word 文件中的 2026 年 2-4 月补充数据。新增数据包括：

```text
企业信息
原始凭证
OCR 解析字段
活动数据
排放因子
行业基准
碳核算结果
ESG 评分
风控结果
报告记录
首页事件流
证据链记录
```

脚本已经做了幂等处理。再次 dry-run 不会重复导入同一批数据。

### 4. 当前数据库关键数量

```text
users: 11
enterprise_profiles: 10
raw_data_record: 26
parsed_data_record: 54
activity_record: 29
emission_factor_items: 34
analysis_results: 35
analysis_breakdowns: 47
industry_benchmarks: 12
esg_scores: 6
risk_results: 108
report_records: 12
event_stream: 35
evidence_chain_record: 205
```

## 三、不要随便删除的文件

接手同学不要随便删除以下文件：

```text
api.js
app.js
demo-flow.js
real-mode.js
home-dynamics.js
visual-system.js
style.css
backend/main.py
backend/routers/system.py
backend/import_supplement_data.py
```

文件作用：

```text
demo-flow.js：原有演示模式。
real-mode.js：真实 API 闭环模式。
home-dynamics.js：首页动态图、事件流、拓扑图、证据链视觉增强。
visual-system.js：图表主题、模态框修复和整体视觉统一。
backend/routers/system.py：真实模式健康检查。
backend/import_supplement_data.py：补充数据导入脚本。
```

## 四、真实模式运行与检查

### 后端启动

```bash
cd backend
python main.py
```

或：

```bash
cd backend
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

### 前端启动

不建议用 `file://` 打开。建议在项目根目录运行：

```bash
python -m http.server 5500
```

然后访问：

```text
http://localhost:5500
```

### 真实模式健康检查

浏览器或接口工具访问：

```text
http://localhost:8000/api/system/real-mode-health
```

如果返回 `ok`，说明关键真实链路可运行。

如果返回 `warning`，说明真实闭环通常可跑，但 OCR 或报告服务可能处于 mock / 降级模式。

如果返回 `blocked`，说明关键依赖不可用，需要先修数据库、因子库或证据链表。

### 前端真实模式入口

首页会出现“真实运行链路”面板，包含：

```text
检查真实服务
运行真实闭环
可选上传票据
真实链路凭证区
```

推荐演示流程：

```text
1. 先点“检查真实服务”
2. 确认数据库、因子库、证据链为正常
3. 再点“运行真实闭环”
4. 展示 OCR、碳核算、风控、存证、核验、报告生成步骤
5. 展示 tx_id / Merkle Root / analysis_id 等真实返回值
```

## 五、后续最优先修改建议

### 1. 继续稳定真实运行模式

当前 Real Mode 已经补上统一健康检查，但后续还可以继续增强：

```text
让首页 KPI 完全来自后端统计接口
让首页事件流完全从 /api/events/latest 拉取
给真实上传票据增加更清晰的失败原因
把 OCR mock / 真实 VLM 状态展示得更明显
增加一键导出真实闭环运行日志
```

### 2. 首页动态图继续优化

当前首页已经有数据流转图，但还可以更精致：

```text
节点尺寸继续缩小
节点颜色更克制
避免文字拥挤
保留拖动联动效果
减少默认 ECharts 味道
KPI hover 联动更明确
```

建议保持主链路：

```text
企业 -> 凭证 -> OCR -> 碳核算 -> 风控 -> 区块链 -> 绿色银行 -> 金融产品
```

不要做成炫酷大屏，要保持“学术型数据平台 + 企业级 SaaS 后台”的气质。

### 3. 碳核算页继续替换 3D 粒子展示

后续建议弱化黑色 Three.js 粒子效果，重点展示碳流向：

```text
外购电力 / 燃料 / 物流 / 包装
-> 采购端 / 生产环节 / 仓储配送
-> Scope 1 / Scope 2 / Scope 3
-> 总碳排
```

建议使用更清晰的 Sankey 图或拓扑流向图，并在每张图下面加一句结论型说明。

### 4. 区块链展示继续业务化

不要只展示：

```text
Hash
Merkle Root
tx_id
```

评委更容易理解的表达是：

```text
数据原始采集
OCR 识别完成
数据标准化完成
碳核算完成
风控扫描完成
链上存证成功
全流程核验通过
```

Hash 可以放在展开详情里，不建议作为页面主视觉。

### 5. 继续减少前端硬编码数据

当前前端仍有一些硬编码数据，例如：

```text
首页 KPI 数字
首页事件流部分模拟数据
图表初始化数据
部分报告预览内容
```

建议逐步改为后端接口拉取：

```text
/api/dashboard/...
/api/events/latest
/api/carbon/records
/api/reports/my-reports
/api/forecast/monthly-carbon
/api/system/real-mode-health
```

目标是让页面看起来真正和数据库联动。

## 六、数据库导入脚本说明

如果需要重新预览补充数据导入计划：

```bash
python backend/import_supplement_data.py
```

这是 dry-run，不会写库。

真正写库：

```bash
python backend/import_supplement_data.py --apply
```

注意：脚本现在已经做幂等处理，重复运行一般不会重复插入同一批数据。

## 七、演示账号

原有账号：

```text
demo@carbon-ai.com
demo123
```

新增补充企业账号：

```text
supplement-e003@carbon.local
supplement-e004@carbon.local
supplement-e005@carbon.local
supplement-e006@carbon.local
supplement-e007@carbon.local
supplement-e008@carbon.local
```

默认密码：

```text
demo123
```

对应企业：

```text
鑫源新材料有限公司
味鲜食品有限公司
云智科技有限公司
盛华化工有限公司
速通物流有限公司
绿建建材有限公司
```

## 八、答辩表述建议

不要说：

```text
这是完全商用级系统
已经接入真实金融机构
已经是真实联盟链
```

更稳妥的说法：

```text
本系统已经实现项目验收级真实可运行闭环。前端保留演示模式，同时提供真实运行模式。真实模式下可以连接 FastAPI 后端和 MySQL 数据库，完成票据识别、活动数据标准化、碳核算、ESG 评分、风控检测、报告生成和证据链存证。当前系统已导入 2026 年 2-4 月补充业务数据，可支撑典型中小企业场景演示；若后续进入生产环境，还需要继续扩展排放因子库、真实联盟链节点、风控模型训练数据和权限审计机制。
```

## 九、建议分工

如果后续还有时间，建议这样分工：

```text
同学 A：负责真实接口联动，把首页 KPI、事件流和业务趋势改成后端拉取。
同学 B：负责碳核算页图表优化，重点做 Sankey、行业基准对比和结论型说明。
同学 C：负责风控与证据链页面，把 Hash 展示改成业务时间线。
同学 D：负责报告中心和导出，完善 PDF 样式和报告模板。
```

## 十、总结

当前项目已经具备真实应用原型能力，不建议推倒重来。

后续重点不是继续堆页面，而是：

```text
减少硬编码
增强真实 API 联动
提高图表学术感
强化证据链业务表达
补充更多排放因子和行业基准
继续稳定 Real Mode 真实运行入口
```

这样系统会从“能演示”进一步变成“能验收、能说明、能扩展”的完整项目。
