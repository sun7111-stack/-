# 碳融智核前后端接口文档（按当前代码基线）

更新时间：2026-04-16  
适用范围：项目主目录（不含 `旧版备份_碳融智核_完整项目`）

---

## 1. 联调基线

- 后端入口：`backend/main.py`
- 后端基地址：`http://localhost:8000`
- 前端 API 基地址：`http://localhost:8000/api`（见 `api.js`）
- 鉴权方式：`Authorization: Bearer <token>`
- Content-Type：
  - JSON 接口：`application/json`
  - 上传接口：`multipart/form-data`

### 1.1 鉴权约定

- 无需登录：`/api/auth/captcha`、`/api/auth/sms-code`、`/api/auth/login*`、`/api/auth/register`、`/api/auth/visitor-preview`、`/api/carbon/emission-factors`、`/api/carbon/industry-benchmarks`、`/api/ocr/health`、`/health`、`/`
- 需要登录：除上面之外大多数业务接口。
- 可选登录（未登录可走降级逻辑）：`/api/risk/detect`、`/api/forecast/monthly-carbon`、`/api/home/graph`、`/api/demo/run-once`

### 1.2 通用响应约定

- 成功：HTTP 2xx，返回 JSON 或结构体对象。
- 失败：HTTP 4xx/5xx，后端常见错误字段为 `detail`。
- 前端统一错误处理：`api.js` 的 `request()` 会优先读取 `detail`，否则给出默认文案。

---

## 2. 前端已封装接口（现状对齐）

以下方法已在 `api.js` 可直接调用。

### 2.1 认证模块

- `POST /api/auth/login`
- `POST /api/auth/register`
- `GET /api/auth/captcha`
- `POST /api/auth/sms-code`
- `POST /api/auth/login/phone`
- `POST /api/auth/login/enterprise`
- `GET /api/auth/visitor-preview`
- `GET /api/auth/me`
- `PUT /api/auth/me`

### 2.2 碳核算模块

- `GET /api/carbon/emission-factors`
- `GET /api/carbon/industry-benchmarks`
- `POST /api/carbon/calculate`
- `GET /api/carbon/records`

补充（后端已实现，前端未封装）：
- `GET /api/carbon/result/{analysis_id}`
- `GET /api/carbon/flow/{analysis_id}`
- `GET /api/carbon/explain/{analysis_id}`
- `GET /api/carbon/records/{record_id}`
- `DELETE /api/carbon/records/{record_id}`

### 2.3 ESG 模块

- `POST /api/esg/calculate/environment`
- `POST /api/esg/calculate/social`
- `POST /api/esg/calculate/governance`
- `POST /api/esg/calculate/total`
- `GET /api/esg/history`

补充（后端已实现，前端未封装）：
- `GET /api/esg/history/{record_id}`
- `GET /api/esg/score-detail`
- `GET /api/esg/benchmark`
- `GET /api/esg/improvement-plans`
- `GET /api/esg/trends`

### 2.4 金融模块

- `GET /api/finance/products`
- `GET /api/finance/products/{id}`
- `POST /api/finance/apply`
- `GET /api/finance/applications`

补充（后端已实现，前端未封装）：
- `GET /api/finance/recommendations`
- `GET /api/finance/progress/{application_id}`
- `GET /api/finance/comparison`
- `GET /api/finance/materials`
- `POST /api/finance/materials`
- `POST /api/finance/batch-apply`

### 2.5 报告与内容模块

- `GET /api/reports/templates`
- `POST /api/reports/generate`
- `GET /api/reports`
- `POST /api/reports/generate-ai`
- `GET /api/policies`
- `GET /api/cases`
- `POST /api/contact`
- `POST /api/reports/{report_id}/save-cloud`（在 `demo-flow.js` 直接调用）

补充（后端已实现，前端未封装）：
- `POST /api/reports/preview-context`
- `POST /api/reports/export`
- `GET /api/reports/export-records`
- `GET /api/reports/my-reports`
- `GET /api/reports/review-list`
- `PUT /api/reports/{report_id}/review`
- `GET /api/reports/custom-templates`
- `POST /api/reports/custom-templates`
- `GET /api/reports/{report_id}`

### 2.6 OCR 模块

- `POST /api/ocr/recognize`（文件上传）
- `GET /api/ocr/sample/{doc_type}`

补充（后端已实现，前端未封装）：
- `GET /api/ocr/health`
- `GET /api/ocr/chain-check/{raw_data_id}`
- `GET /api/ocr/chain-overview`

### 2.7 风控、证据链、预测模块

- `POST /api/risk/detect`
- `POST /api/evidence/store`
- `GET /api/evidence/chain/{record_id}`
- `POST /api/evidence/verify`
- `GET /api/evidence/proof/{leaf_id}`
- `POST /api/forecast/monthly-carbon`

补充（后端已实现，前端未封装）：
- `GET /api/home/graph`
- `GET /api/events/latest`
- `POST /api/demo/run-once`

### 2.8 驾驶舱与碳管理（后端已实现，前端建议补封装）

- 驾驶舱：
  - `GET /api/dashboard/emission-monitor`
  - `GET /api/dashboard/energy-structure`
  - `GET /api/dashboard/esg-board`
  - `GET /api/dashboard/key-indicators`
- 碳管理：
  - `GET /api/carbon-manage/uploads`
  - `POST /api/carbon-manage/upload`
  - `DELETE /api/carbon-manage/uploads/{upload_id}`
  - `POST /api/carbon-manage/manual-entry`
  - `GET /api/carbon-manage/energy-analysis`

---

## 3. 关键接口请求/响应示例

## 3.1 登录

`POST /api/auth/login`

请求：
```json
{
  "email": "demo@carbon-ai.com",
  "password": "demo123"
}
```

响应（示意）：
```json
{
  "access_token": "<jwt>",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "name": "演示用户",
    "company": "演示企业"
  }
}
```

## 3.2 OCR 识别

`POST /api/ocr/recognize`

请求：`multipart/form-data`，字段名 `file`

响应（示意）：
```json
{
  "success": true,
  "data": {
    "doc_type": "electricity_bill",
    "confidence": 0.96,
    "fields": {"amount": 1320, "unit": "kWh"},
    "suggested_activity_type": "electricity",
    "chain_ids": {
      "raw_data_id": 12,
      "parsed_ids": [31, 32],
      "activity_id": 9
    }
  },
  "message": "识别成功"
}
```

## 3.3 碳核算

`POST /api/carbon/calculate`

请求（示意）：
```json
{
  "enterprise_name": "绿能科技",
  "annual_revenue": 1200,
  "company_type": "manufacture",
  "suggested_activity_type": "electricity",
  "fields": {"amount": 1320},
  "activity_data": {
    "electricity": 1320,
    "diesel": 200
  }
}
```

响应（示意）：
```json
{
  "total_emission": 2850.4,
  "breakdown": [
    {"item": "electricity", "emission": 710.0}
  ],
  "benchmark_compare": {
    "industry_avg": 3100.0,
    "deviation_ratio": -0.081,
    "position": "低于行业平均 8.1%"
  }
}
```

## 3.4 风控检测

`POST /api/risk/detect`

请求（示意）：
```json
{
  "task_id": "TASK-20260416-001",
  "bill_id": "BILL-001",
  "raw_text": "票据原文摘要",
  "structured_fields": {"esg_score": 78},
  "total_emission": 53.2,
  "carbon_intensity": 0.61,
  "monthly_variation": 0.2
}
```

响应（示意）：
```json
{
  "success": true,
  "task_id": "TASK-20260416-001",
  "is_anomaly": false,
  "risk_score": 41.2,
  "risk_score_explain_v2": 45.8,
  "risk_level": "medium",
  "trust_score": 0.86,
  "risk_reasons": ["运输排放波动偏高"],
  "risk_advice": ["优化运输路径与装载率"],
  "blockchain_hash": "0x...",
  "trace_saved": true
}
```

## 3.5 证据链写入与验真

`POST /api/evidence/store`

请求（示意）：
```json
{
  "analysis_id": "AN-20260416-001",
  "evidence_objects": [
    {
      "step_name": "ocr_done",
      "object_type": "ocr_result",
      "payload": {"doc_type": "electricity_bill", "amount": 1320}
    },
    {
      "step_name": "carbon_done",
      "object_type": "carbon_result",
      "payload": {"total_emission": 2850.4}
    }
  ]
}
```

响应（示意）：
```json
{
  "success": true,
  "analysis_id": "AN-20260416-001",
  "stored_objects": 2,
  "leaf_ids": ["leaf_...", "leaf_..."],
  "merkle_root": "0x...",
  "tx_id": "tx_...",
  "trust_score": 0.92
}
```

`POST /api/evidence/verify`

请求（按分析链验真）：
```json
{
  "analysis_id": "AN-20260416-001"
}
```

## 3.6 月度预测

`POST /api/forecast/monthly-carbon`

请求：
```json
{
  "months_ahead": 6,
  "alpha": 0.1,
  "use_demo_if_empty": true
}
```

响应（示意）：
```json
{
  "success": true,
  "history_count": 10,
  "forecast_count": 6,
  "model": "random_forest",
  "history": [{"month": "2025-07", "value": 9.8}],
  "forecast": [{"month": "2026-05", "value": 13.9}],
  "intervals": [{"month": "2026-05", "lower": 12.7, "upper": 15.2}],
  "high_risk_months": ["2026-08"]
}
```

---

## 4. 前端需要修改的内容（重新整理，按 1 号同学职责）

说明：你当前项目是原生 HTML/CSS/JS，不是 Vue/React。以下改造按当前目录直接落地。

## 4.1 总目标（保持不变，落地方式调整）

1. 全局导航结构重组（分组折叠）  
2. 首页升级为动态业务驾驶舱（真实接口驱动）  
3. 全局视觉规范统一（变量化+组件化）

## 4.2 代码文件级改造清单

### A. 全局 Layout 重构

涉及文件：`index.html`、`style.css`、`app.js`、`router.js`

要做：
- 新增统一壳层：`app-shell`、`app-sidebar`、`app-header`、`app-content`。
- 左侧菜单改为分组折叠：
  - 数据采集端
  - 智能核算引擎
  - 风控与报告中心
  - 金融产品超市
- 顶部栏只保留：平台名、企业切换/企业信息、通知、用户入口。
- 删除或弱化无业务意义按钮（如主题切换可降级到设置页）。
- 统一容器规则：
  - `max-width`
  - `padding-inline`
  - 页面标题层级
  - 卡片间距

验收标准：全站页面宽度、标题层级、卡片留白一致。

### B. 首页“动态业务驾驶舱”重做

涉及文件：`index.html`、`style.css`、`demo-flow.js`、`api.js`

要做：
- 顶部欢迎区：平台名称 + 简介 + 按钮（开始体验、示例案例）。
- 主视觉区（力导向图）：改为后端接口驱动，不用纯静态 mock。
  - `GET /api/home/graph`
- 指标卡片区（4 张）：
  - 累计处理文件
  - 已完成碳核算
  - 已生成智能报告
  - 成功对接金融产品
  - 数据来源：优先 `GET /api/dashboard/key-indicators`，不足字段先本地聚合补齐。
- 事件流区：
  - `GET /api/events/latest?limit=8`
  - 先轮询（5~10s），后续可升级 SSE/WebSocket。
- 最近任务区：
  - 先用 `events/latest` 与现有记录拼装。
  - 后续可新增后端任务接口再替换。

组件建议（原生 JS 版）：
- `renderForceGraphPanel()`
- `renderAnimatedStatCard()`
- `renderEventTicker()`
- `renderRecentTaskList()`
- `renderQuickActionPanel()`

交互联动要求：
- 悬停指标卡时，高亮图谱对应类别节点（enterprise/evidence/engine/risk/finance）。

### C. 证据链可视化（评委可理解）

涉及文件：`index.html`、`style.css`、`demo-flow.js`

要做：
- `EvidenceTimeline`：把“上传 -> OCR -> 核算 -> 风控 -> 存证”展示为时间轴。
- `EvidenceDrawer`：点击节点展开详情。
- `EvidenceStatusCard`：显示验证状态、trust score、merkle root 摘要。

接口对接：
- 存证：`POST /api/evidence/store`
- 链路：`GET /api/evidence/chain/{record_id}`
- 验真：`POST /api/evidence/verify`
- 证明：`GET /api/evidence/proof/{leaf_id}`

抽屉信息建议字段：
- 企业名称
- 凭证编号/分析 ID
- 存证时间
- 哈希摘要
- 链上状态
- 风险评分与可信分

### D. 全局视觉规范落地（Design Tokens）

涉及文件：`style.css`

要做：
- 定义 CSS 变量（必须）：
  - 主色（深绿）
  - 辅助色（灰蓝/深灰）
  - success/warning/danger/info
  - 字体层级、圆角、阴影、边框、间距
- 建立统一卡片类：`.app-card`、`.app-card--hover`
- 建立统一标签类：`.status-badge` + `is-high|is-medium|is-low|is-done|is-processing`
- 统一按钮和表格样式，避免局部“各写一套”

建议最小变量集合：
```css
:root {
  --brand-700: #1b5e20;
  --brand-600: #2e7d32;
  --text-900: #1f2937;
  --text-600: #4b5563;
  --bg-50: #f7faf7;
  --info: #2563eb;
  --warn: #f59e0b;
  --danger: #dc2626;
  --success: #16a34a;
  --radius-lg: 16px;
  --shadow-card: 0 8px 24px rgba(0,0,0,.08);
}
```

---

## 5. 你当前前端的“必改项”（P0）

1. `api.js` 新增并统一方法，减少 `demo-flow.js` 里 `API.request('/xxx')` 的散调用。  
2. 首页图谱与事件流改成真实接口驱动（`/api/home/graph`、`/api/events/latest`）。  
3. 证据链区域从“哈希文本”改成“时间轴 + 抽屉 + 验真状态”。  
4. 抽离全局 layout 样式，避免页面间风格和宽度不一致。  
5. 清理重复/历史兼容代码（重点：`api.js` 里重复定义的 `calculateCarbon`）。

---

## 6. 建议新增的前端 API 封装（直接可抄）

在 `api.js` 增补：

```js
async getHomeGraph() {
  return this.get('/home/graph');
},

async getLatestEvents(limit = 8) {
  return this.get(`/events/latest?limit=${limit}`);
},

async runDemoOnce() {
  return this.post('/demo/run-once', {});
},

async getDashboardEmissionMonitor() {
  return this.get('/dashboard/emission-monitor');
},

async getDashboardEnergyStructure() {
  return this.get('/dashboard/energy-structure');
},

async getDashboardESGBoard() {
  return this.get('/dashboard/esg-board');
},

async getDashboardKeyIndicators() {
  return this.get('/dashboard/key-indicators');
},

async getOCRChainCheck(rawDataId) {
  return this.get(`/ocr/chain-check/${rawDataId}`);
},

async getOCRChainOverview() {
  return this.get('/ocr/chain-overview');
}
```

---

## 7. 联调验收清单（前端视角）

- 登录后能获取并缓存 token，刷新后仍可调用需要鉴权接口。
- 首页四区（图谱/指标/事件/任务）均有真实数据渲染。
- 演示主流程“上传 -> 核算 -> 风控 -> 报告 -> 存证 -> 验真”能完整闭环。
- 预测图在首次展示时不空白，区间切换有效。
- 同一业务动作不出现重复按钮、重复绑定、重复请求。

---

## 8. 备注

- 本文以主目录当前后端路由与前端封装为准。  
- 旧版备份目录仅作历史参考，不作为联调依据。
