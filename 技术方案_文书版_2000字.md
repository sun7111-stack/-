# 碳融智核技术方案（文书版，约2000字）

## 一、建设背景与目标

在“双碳”政策与 ESG 披露要求持续强化的背景下，中小企业面临三重现实约束：一是碳数据采集碎片化，票据、台账、业务系统之间缺少统一口径；二是核算过程复杂，因子匹配、范围边界、质量评估高度依赖人工经验；三是对外披露与融资对接缺少可信证据，导致“有数据但难被采信”。

碳融智核以“低门槛接入、全流程自动化、可解释与可验证并重”为设计原则，建设一套从数据采集到报告输出再到金融对接的 ESG 数字化平台，帮助企业实现：
1. 数据采集标准化；
2. 碳核算自动化；
3. 风险识别前置化；
4. 披露证据可信化；
5. 融资对接高效化。

---

## 二、总体架构设计

平台采用四层架构：

1. 展示交互层
- 基于 HTML/CSS/JavaScript 构建业务门户与演示流程；
- 使用 ECharts、Chart.js 展示趋势、结构、风险等可视化结果；
- 提供报告页、预测页、风险页、驾驶舱等多场景视图。

2. 业务服务层
- 基于 FastAPI 构建 RESTful API；
- 通过模块化路由拆分认证、核算、ESG、金融、OCR、风险、报告、证据链、预测等能力；
- 借助 JWT 完成会话与权限控制。

3. 算法与智能层
- 策略模式碳核算引擎；
- Isolation Forest 风险检测；
- 时序预测与 Conformal 区间预警；
- LLM 结构化报告生成（Qwen-Plus）；
- VLM/OCR 票据识别（Qwen-VL + 规则兜底）。

4. 数据与可信层
- MySQL 存储业务数据；
- SQLAlchemy 实现模型映射与事务管理；
- 证据链服务提供哈希链、Merkle 证明与可信度评分，增强可审计性。

---

## 三、核心技术选型与理由

1. FastAPI + SQLAlchemy
- FastAPI 具备高性能、文档自动化、类型校验优势，适合中后台接口场景；
- SQLAlchemy 便于复杂业务模型演进与多表关联维护。

2. MySQL
- 成熟稳定，易运维，适合结构化业务数据与审计数据的长期管理。

3. ECharts / Chart.js
- 满足仪表盘、趋势预测、区间带、雷达图等多样可视化表达。

4. DashScope（Qwen）
- 在中文业务文本生成与结构化输出方面表现稳定，可直接服务企业报告场景。

5. scikit-learn / xgboost
- 兼顾模型可解释性与工程可落地性；
- 当 xgboost 不可用时自动回退 RandomForest，提升系统韧性。

6. 证据链机制（哈希+Merkle）
- 以轻量方式实现“数据可验真”，满足申报、审计、尽调等场景对可信性的要求。

---

## 四、功能模块设计

## 4.1 数据采集与 OCR 识别

平台支持图片与 PDF 票据上传。识别流程采用“复杂度识别 + 模型解析 + 兜底策略”：
1. 先判断文档复杂度（simple/standard/complex）；
2. 常规场景走启发式提取与规则识别；
3. 复杂场景调用 VLM 进行多模态解析；
4. 模型不可用时回退 Mock 或规则结果，保证流程不断裂。

输出包含 doc_type、confidence、fields、parse_method、complexity 等字段，为后续治理与核算提供标准输入。

## 4.2 数据治理与标准化

针对 OCR 输出存在的异构性，平台进行四步治理：
1. 字段映射：将业务字段映射到核算统一口径；
2. 单位归一：统一 kWh、km、L、t·km 等单位；
3. 合法性校验：识别负值、异常大值、缺失值；
4. 活动记录构建：生成可直接参与核算的数据结构。

该模块将“原始识别结果”转化为“可计算、可追踪、可审计”的活动数据。

## 4.3 碳核算引擎（策略模式）

核算引擎按企业类型选择策略（默认、跨境、日用百货），并结合因子库进行自动匹配与计算。核心能力包括：
1. 多活动类型核算；
2. Scope1/2/3 自动分类；
3. 分项排放与总量汇总；
4. 行业基准对标；
5. 不确定性区间估计；
6. 碳流图与解释链路输出。

因子匹配采用分层回退机制（精确匹配 -> 地域回退 -> 行业回退 -> 默认值），可有效提升复杂场景下的可用性。

## 4.4 风险检测与解释

风控模块基于 Isolation Forest，对碳排与运营特征进行异常识别。输入特征覆盖用电、物流、退货率、碳强度等基础指标，并扩展能耗强度、Scope3 占比、行业偏离等增强特征。

输出不止风险分，还包含：
1. 风险等级；
2. 异常原因中文解释；
3. 优化建议；
4. 特征重要性。

这使得结果可直接转化为管理动作，而非停留在“黑盒打分”。

## 4.5 AI 报告生成

报告模块采用结构化输入与结构化输出设计：
- 输入汇聚核算、风控、证据链、同比趋势等业务数据；
- 输出固定为五段结构：执行摘要、关键发现、风险解释、可信度说明、优化建议。

系统内置 JSON 容错解析与 Mock 回退，确保在外部模型波动时仍可稳定给出可用报告。

## 4.6 证据链与可信度体系

平台在关键步骤写入证据对象，形成链式摘要并构建 Merkle Root 与 Proof。验真模块可对链路完整性与证明路径进行校验，并计算 trust_score（完整性、哈希、时间、签名四个维度）。

该机制可支撑：
1. ESG 披露材料可信增强；
2. 银行尽调“可追溯”要求；
3. 项目申报“过程可核验”要求。

## 4.7 时序预测与预警

预测模块对月度碳排数据做滑窗监督学习，输出未来多月预测均值与区间上/下界。区间采用 Split-Conformal 残差分位数方法，强调不确定性可视化。

当历史样本不足时，系统自动回退基线模型，避免“无结果”影响业务连续性。并提供动态阈值与高风险月份清单，支持管理层提前干预。

## 4.8 金融对接

金融模块提供产品列表、推荐、申请、进度跟踪与材料管理。推荐逻辑结合 ESG 表现、碳效水平等指标计算匹配度，使“核算与报告结果”可以直接转化为“融资行动”。

---

## 五、数据模型与主链路

核心数据对象包括：
1. 原始数据记录、解析数据记录、活动记录；
2. 核算结果、分项结果、风险结果、ESG 结果；
3. 报告记录与导出记录；
4. 证据对象、链式步骤、锚定信息、证明路径；
5. 金融产品与申请记录。

业务主链路如下：
上传/录入 -> 识别 -> 治理 -> 核算 -> 风险 -> 报告 -> 存证 -> 预测 -> 融资申请。

该链路在系统内部形成闭环，并通过 API 与可视化统一呈现。

---

## 六、工程实现与运维要点

1. 服务启动
- 后端：uvicorn main:app --reload --host 0.0.0.0 --port 8000
- 前端：python -m http.server 5500

2. 配置管理
- 使用 .env 管理数据库、JWT、CORS、模型密钥；
- 可通过 /health 与 /docs 进行状态检查与接口联调。

3. 安全与稳定
- JWT 鉴权、密码加密、参数校验；
- 模型与预测均有回退策略；
- 关键链路具备可追踪与可验真能力。

4. 可扩展性
- 路由模块化，新增业务可按域扩展；
- 核算策略可按行业继续新增；
- 因子库支持版本化管理；
- 预测与风控模块可替换为更复杂模型。

---

## 七、项目创新点与应用价值

## 7.1 创新点
1. 将“识别-治理-核算-风控-报告-存证-预测”串成单一产品闭环；
2. 报告采用结构化大模型输出，便于业务系统消费与审阅；
3. 证据链与 Merkle 校验引入 ESG 披露场景，提升数据可信性；
4. 预测结果同时提供区间与阈值预警，支持管理决策前置。

## 7.2 应用价值
1. 降低企业 ESG 数据管理与披露的人力成本；
2. 缩短绿色融资材料准备周期；
3. 提高企业碳管理透明度与审计通过率；
4. 为园区、银行、监管等多方协同提供统一数字底座。

---

## 八、结论

碳融智核已具备从数据采集到融资转化的工程化能力，既能满足中小企业当前的合规与管理诉求，也具备向行业化、平台化扩展的技术基础。其核心优势在于“实用闭环 + 可信机制 + 智能能力融合”，适合在绿色制造、跨境电商、供应链协同等场景中推广应用。